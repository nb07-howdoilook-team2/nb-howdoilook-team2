version: "3.8"

services:
  # 💡 요청하신 호스트명에 맞춰 서비스 이름을 'postgres_container'로 설정합니다.
  postgres_container:
    image: postgres:15-alpine
    restart: always
    environment:
      POSTGRES_USER: postgres # ⬅️ URL의 Username: postgres
      POSTGRES_PASSWORD: postgres # ⬅️ URL의 Password: postgres
      POSTGRES_DB: test1 # ⬅️ URL의 DB Name: test1
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data/

  # 💡 Node.js 코드를 실행하는 개발 환경 컨테이너 (이전에 존재했다고 가정)
  dev:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app
    depends_on:
      - postgres_container # DB 컨테이너에 의존하도록 설정
    ports:
      - "3000:3000"

volumes:
  postgres_data:
